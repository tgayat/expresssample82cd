name: Trigger auto deployment for appspacesragapp901fb68fa29d

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/appspacesragapp901fb68fa29d-AutoDeployTrigger-3476daba-391f-4947-9f7f-8b1f5b73256b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.APPSPACESRAGAPP901FB68FA29D_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.APPSPACESRAGAPP901FB68FA29D_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.APPSPACESRAGAPP901FB68FA29D_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.APPSPACESRAGAPP901FB68FA29D_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.APPSPACESRAGAPP901FB68FA29D_REGISTRY_PASSWORD }}
          containerAppName: appspacesragapp901fb68fa29d
          resourceGroup: appspacesragapp901fb68f
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


